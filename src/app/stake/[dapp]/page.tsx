"use client";

import APriori from "@/sections/aPriori";
import { useParams } from "next/navigation";

export default function SwapPage() {
  const urlParams = useParams();
    

  return <APriori />;
}
