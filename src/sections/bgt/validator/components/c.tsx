import useIsMobile from '@/hooks/use-isMobile';

const Com = (props: any) => {
  const {} = props;
  const isMobile = useIsMobile();

  return (
    <div className="">

    </div>
  );
};

export default Com;
