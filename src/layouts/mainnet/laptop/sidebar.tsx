const LaptopSidebar = (props: any) => {
  const { className } = props;

  return (
    <div className="fixed z-[9] left-0 top-0 w-[226px] h-full bg-black/30 backdrop-blur-[10px] shrink-0">

    </div>
  );
};

export default LaptopSidebar;
